# Assets

`brain-report.svg` is the real output of `wintersolve brain .` on this repository, rendered as a terminal image (colour is added for the picture only; the report itself is plain text). Regenerate it after notable changes to the report format.
